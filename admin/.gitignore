# The root .gitignore anchors its patterns to the repository root
# (/node_modules, /.next/), so they do not reach this app. Ignoring here keeps
# the rules next to the thing they describe.

# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# env files (can opt-in for committing if needed)
.env*
!.env*.example

# Regenerated by `next dev`. See the note in the repository root.
AGENTS.md
CLAUDE.md
